
module.exports = {

    // for anything that doesn't have an explicit mapping
    __default: () => new Object(),

    // i.e. if you want any <team> to translate to a Team class
    // team: () => new Team(),

}
